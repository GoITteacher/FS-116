/**
 * Додавання прослуховувача подій на кожен елемент
 *
 * Отримай колір квадратика по якому було здійснено клік
 */

// const container = document.querySelector('.container');

// container.children[0].addEventListener('click', e => {
//   console.log(e.target.dataset.color);
// });
// container.children[1].addEventListener('click', e => {
//   console.log(e.target.dataset.color);
// });
// container.children[2].addEventListener('click', e => {
//   console.log(e.target.dataset.color);
// });

//!======================================================

/**
 * Дегегування подій
 *
 * Отримай колір квадратика по якому було здійснено клік
 */

// const container = document.querySelector('.container');

// container.addEventListener('click', e => {
//   //   if (e.target === container) return;
//   //   if (e.target === e.currentTarget) return;

//   console.log('Hello');
// });

//!======================================================

// const btnListElem = document.querySelector('.btn-list');

// btnListElem.addEventListener('click', e => {
//   //   if (e.target === e.currentTarget) return;
//   //   if (e.target.nodeName !== 'BUTTON') return;
//   //   if (!e.target.classList.contains('add-btn')) return;
//   //   if (e.target.dataset.type !== 'add') return;
//   //   if (e.target.textContent !== 'add') return;
//   //   console.log(e.target.nodeName);
// });
