/*
 * Властивості елемента (hero)
 * - Зображення
 * - Текст та textContent
 * https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480
 */
//!======================================================
/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

// const imgElem = document.querySelector('img');

// const random = Math.round(Math.random() * 100);

// imgElem.setAttribute('src', `https://picsum.photos/720?random=${random}`);
// imgElem.setAttribute('alt', `Image ${random}`);

// imgElem.setAttribute('name', 'my-image');
// console.log(imgElem);

// const imgSrc = imgElem.getAttribute('src');
// console.log(imgSrc);

// if (imgElem.hasAttribute('disabled')) {
//   console.log('DISABLE IMAGE');
// }

//!======================================================
/*
 * Data-атрибути
 */

// const actions = document.querySelectorAll('.actions button');
// console.log(actions);
// const firstBtn = actions[2];
// console.log(firstBtn.dataset);
// console.log(firstBtn.dataset.action);
// firstBtn.dataset.title = 'Hello world';

//!======================================================
